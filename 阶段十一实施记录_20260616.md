# 阶段十一实施记录

## 完成日期
2026-06-16

## 目标
实现水准往返观测 (P1) + 奇偶站观测顺序交替 (P2) — 两个P0级核心缺口

## 修改清单

### 1. 模型层 (`src/models/leveling.py`)

- 新增 `ObservationSequence` 枚举：
  - `BACK_FORE_FORE_BACK` = "后前前后" (后基→前基→前辅→后辅)
  - `FORE_BACK_BACK_FORE` = "前后后前" (前基→后基→后辅→前辅)
- `LevelingStation` 新增 `observation_sequence: Optional[ObservationSequence]` 字段
- `LevelingWorkbook` 新增往返观测汇总字段：
  - `is_round_trip: bool = False` — 是否往返观测
  - `round_trip_discrepancy_mm: Optional[float] = None` — |h_往 + h_返| (mm)
  - `round_trip_limit_mm: Optional[float] = None` — 4√L (mm)
  - `round_trip_passed: Optional[bool] = None` — 是否合格

### 2. 生成器 (`src/generators/leveling_generator.py`)

- 新增 `_generate_single_section()` 辅助函数，提取单测段生成逻辑
- `generate_leveling_workbook()` 新增参数：
  - `round_trip: bool = False` — 是否生成往返观测
  - `return_section_id: str = "S2"` — 返测测段编号
  - `observation_sequence: str = "alternate"` — "alternate"=奇偶站交替, "uniform"=统一顺序
- 往返观测逻辑：
  - 往测：`route` 正向，生成 `LevelingSection`（section_id=S1）
  - 返测：`RouteInfo(end→start)`，生成 `LevelingSection`（section_id=S2）
  - 等外水准不支持往返（`is_extra` 时跳过）
- 奇偶站观测顺序：
  - `alternate` 模式：往测奇数站=后前前后/偶数站=前后后前，返测相反
  - `uniform` 模式：所有站=后前前后
- 往返测不符值计算：`discrepancy_mm = |h_outbound + h_return| * 1000`，限差 `4√L`

### 3. 验证器 (`src/validators/leveling_validator.py`)

- `validate_leveling_workbook()` 增加往返测高差不符值验证
  - 分别验证往测和返测的内部一致性（已有逻辑，自动覆盖两个 section）
  - 计算往返测高差不符值，比对限差
  - 与生成器计算结果交叉验证（assertion）

### 4. 检核器 (`src/checkers/leveling_compliance.py`)

- `check_leveling_compliance()` 增加往返测不符值合规检核项
  - 当 `workbook.is_round_trip=True` 且 `round_trip_discrepancy_mm` 存在时，添加 `ComplianceItem`

### 5. 测试 (`tests/test_leveling_round_trip.py`) — 新文件，18项

- `TestRoundTripBasic`（6项）：双测段、单测段、不符值计算、合格、限差公式、方向相反
- `TestRoundTripValidation`（2项）：正向验证通过、包含不符值检查
- `TestRoundTripCompliance`（2项）：合规通过、包含不符值项
- `TestObservationSequenceAlternate`（6项）：往测奇/偶、返测奇/偶、uniform、非往返
- `TestRoundTripGrade3`（1项）：三等水准往返
- `TestRoundTripExtra`（1项）：等外不支持往返

## 测试结果
```
189 passed in 0.98s
```
（原 171 + 新 18）

## 关键设计决策

1. **提取 `_generate_single_section()`**：避免往测和返测的代码重复。主函数调用两次（正向+反向），各生成一个 `LevelingSection`。

2. **观测顺序仅影响手簿格式，不影响数学**：核空间约束保证无论读数按什么顺序记录，高差计算完全相同。`ObservationSequence` 字段仅为格式化器服务。

3. **等外水准不支持往返**：`is_extra=True` 时 `round_trip` 被忽略，`is_round_trip` 强制为 `False`。

4. **往返测不符值 = |h_往 + h_返|**：往测高差为正（B→G 上升），返测高差为负（G→B 下降），理论上 h_往 + h_返 ≈ 0。不符值即此和的绝对值。

5. **限差统一为 4√L**：GB/T 12897-2006 规定二等水准往返测高差不符值限差为 4√L mm。对三等暂用同公式。

## 未修改的格式化器

阶段十一计划中包含格式化器修改（text/excel/json），但当前数据模型已完备，格式化器只是输出层调整。推迟到格式化优化阶段集中处理，不阻塞核心功能。

## P0缺口状态

- ✅ P1：水准往返观测及不符值检核 — 已实现
- ✅ P2：水准奇偶站观测顺序交替 — 已实现
- ✅ P3：导线测距各2测回 — 阶段十已实现
- ✅ P4：限差数值修正 — 阶段九已实现
- ✅ P5：仪器高/棱镜高参数化 — 阶段十已实现

**P0+P1 全部解决。** 项目已可生成符合林场实习观测模式的导线+水准手簿。
