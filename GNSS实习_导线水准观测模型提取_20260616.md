# GNSS实习 — 导线观测与水准观测模型提取

> 来源文件：
> 1. `第3组附合导线控制测量设计书.md`
> 2. `第3组水准控制测量.md`
> 3. `第3组林场实习技术总结_20250628.docx`

---

## 一、附合导线观测模型

### 1. 几何模型

**附合导线**：从一个已知边（含方位角）出发，经过若干未知导线点，最终附合到另一个已知边上。

- **起点/终点**：B → G
- **方位基准点**：B2（起点方位基准）、G2（终点方位基准）
- **路线**：B → K1 → K2 → … → K9 → G
- **导线总长**：约 2.0–2.3 km（设计 2.3 km，实际约 2 km）
- **测站数**：设计 11 站，实际 15+ 中间点

### 2. 观测量

| 观测量 | 方法 | 精度/要求 |
|--------|------|-----------|
| **水平角** β | 测回法（方向观测法），盘左盘右 | 2 测回，半测回较差 ≤ 9″，测回间较差 ≤ 10″，测角中误差 ≤ 5″ |
| **斜距** S | 电磁波测距（全站仪） | 往返各 2 测回，一测回读数较差 ≤ 10 mm，测距中误差 ≤ 15 mm |
| **仪器高** i / **棱镜高** v | 钢尺量取 | 精度 ±0.5 mm |

### 3. 数据处理模型

#### 3.1 边长改正

测量的斜距 S 需经以下改正后化为水平距离 D：

1. **气象改正**：根据现场温度 t、气压 p 计算改正
2. **加常数改正**：仪器加常数 C
3. **乘常数改正**：仪器乘常数 R
4. **倾斜改正**：由斜距 S 和竖角（或仪器高 i、棱镜高 v、高差 h）计算水平距离

$$D = S \cdot \cos\alpha = \sqrt{S^2 - h^2}$$

#### 3.2 角度闭合差计算

附合导线的角度闭合差 $f_\beta$：

$$f_\beta = \sum\beta_{\text{实测}} - \sum\beta_{\text{理论}}$$

其中理论角度和由已知方位角推算：

$$\sum\beta_{\text{理论}} = \alpha_{\text{终}} - \alpha_{\text{起}} + n \times 180°$$

**限差**：$f_\beta \leq 10\sqrt{n}$（n 为测站数）

实际成果：角度闭合差 9″（合格）

#### 3.3 坐标增量闭合差与调整

坐标增量计算：

$$\Delta X_i = D_i \cdot \cos\alpha_i, \quad \Delta Y_i = D_i \cdot \sin\alpha_i$$

闭合差：

$$f_X = \sum\Delta X_{\text{实测}} - \Delta X_{\text{理论}}, \quad f_Y = \sum\Delta Y_{\text{实测}} - \Delta Y_{\text{理论}}$$

导线全长闭合差：

$$f_D = \sqrt{f_X^2 + f_Y^2}$$

导线全长相对闭合差：

$$K = \frac{f_D}{\sum D} \leq \frac{1}{15000}$$

实际成果：相对闭合误差约 1:16000（合格）

当导线长度 < 标准长度的 1/3 时，绝对闭合差 ≤ 0.13 m。

#### 3.4 严密平差

使用平差软件进行严密平差，输出：
- 单位权中误差
- 点位中误差
- 误差椭圆
- 最弱边相对中误差

### 4. 质量控制限差汇总

| 项目 | 限差 |
|------|------|
| 半测回较差 | ≤ 9″ |
| 测回间较差 | ≤ 10″ |
| 测角中误差 | ≤ 5″ |
| 方位角闭合差 | ≤ 10√n |
| 一测回读数较差（测距） | ≤ 10 mm |
| 各测回间较差（测距） | ≤ 15 mm |
| 测距相对中误差 | ≤ 1/30000 |
| 导线全长相对闭合差 | ≤ 1/15000 |
| 导线绝对闭合差（短导线） | ≤ 0.13 m |

---

## 二、水准观测模型

### 1. 几何模型

**附合水准路线**：从一个已知高程点出发，沿导线点进行水准测量，附合到另一个已知高程点。

- **路线**：沿一级导线点 B → K1 → … → K9 → G
- **等级**：二等水准
- **需加设转点**：因视线长度 ≤ 50 m，相邻导线点间距远大于 50 m，必须在中间加设转点
- **测站数**：设计 21 站（含转点）

### 2. 观测方法

**往返观测**：使用同一套仪器和标尺完成整条路线的往测和返测。

#### 2.1 测站观测顺序

| | 奇数站 | 偶数站 |
|------|--------|--------|
| **往测** | 后基 → 前基 → 前辅 → 后辅（后-前-前-后） | 前基 → 后基 → 后辅 → 前辅（前-后-后-前） |
| **返测** | 前-后-后-前 | 后-前-前-后 |

> "基" = 基本分划，"辅" = 辅助分划

#### 2.2 测站设置要求

| 项目 | 要求 |
|------|------|
| 视线长度 | ≤ 50 m |
| 前后视距差 | ≤ 1.0 m |
| 前后视距累积差 | ≤ 3.0 m |
| 视线高度 | ≥ 0.3 m |

### 3. 数据处理模型

#### 3.1 测站高差计算

每一测站的高差由基本分划和辅助分划分别计算：

$$h_{\text{基}} = a_{\text{后基}} - a_{\text{前基}}, \quad h_{\text{辅}} = a_{\text{后辅}} - a_{\text{前辅}}$$

检核：基辅分划所测高差之差 ≤ 0.6 mm

最终取中数：$h = \frac{h_{\text{基}} + h_{\text{辅}}}{2}$

#### 3.2 各项改正计算

1. **标尺尺长改正**：根据标尺检定的尺长改正方程计算
2. **温度、气压改正**：根据现场气象数据
3. **正常位水准面不平行改正**和**重力异常归算改正**

#### 3.3 往返测高差不符值检核

往返测高差不符值限差：

$$\Delta h_{\text{限}} = 4\sqrt{L} \text{ (mm)}$$

其中 L 为测段长度（km），不足 0.1 km 按 0.1 km 计算。

#### 3.4 高程计算

以 GNSS 平差报告中的起点高程为起算数据：

$$H_{i} = H_{\text{起}} + \sum h$$

本次实习不要求计算高差闭合差，以往返测高差不符值作为数据质量检核标准。

#### 3.5 严密平差

使用平差软件进行严密平差，输出：
- 单位权中误差
- 每公里高差全中误差

### 4. 精度指标与限差汇总

| 项目 | 限差 |
|------|------|
| 每千米偶然中误差 M | ≤ 1.0 mm |
| 每千米全中误差 m | ≤ 2.0 mm |
| 基辅分划读数差 | ≤ 0.4 mm |
| 基辅分划所测高差差 | ≤ 0.6 mm |
| 检测间歇点高差差 | ≤ 1.0 mm |
| 0.5cm 刻划标尺上下丝均值与中丝差 | ≤ 1.5 mm |
| 1cm 刻划标尺上下丝均值与中丝差 | ≤ 3.0 mm |
| 往返测高差不符值 | ≤ 4√L mm |

---

## 三、导线与水准的关联

1. **空间关系**：水准路线沿导线点布设，水准点即为导线点（含转点）
2. **起算数据**：导线起算坐标和水准起算高程均来源于 GNSS 平差结果（自由网平差）
3. **已知点**：B、B2、G、G2 四个点同时作为导线方位基准和水准起算/检核点
4. **实际偏差**：设计 9 个中间点，实际导线选点 15 个（通视条件限制）；水准因视距限制大量加设转点
